//! Preview rendering functions
//!
//! Stubbed during mkui migration. Will use renderer downcasting for
//! attached surface rendering on GUI backends.

use super::PreviewContent;

/// Render preview content (stubbed)
pub fn render_preview(_content: &PreviewContent) {
    // Preview rendering stubbed during migration
}
